# MCMC Sampling

This directory contains the scripts to reproduce the posterior samples.

To run sampling, for example call

```bash
python run.py --data_name="G13_LUAD_12"
```

For more info on the parameters, call

```bash
python run.py --help
```

The results will be stored in `./results`.

